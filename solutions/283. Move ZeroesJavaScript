/**
 * @param {number[]} nums
 * @return {void} Do not return anything, modify nums in-place instead.
 */
var moveZeroes = function(nums) {
let sub_names=[];
    let j=0;
    let l=nums.length;
    
        for (i = 0; i < l; i++) {
           
             if (nums[i]==0){
                 nums.push(0);
                // nums.splice(i, 1);
                 nums[i]=undefined;
                 }
        
    }
  
    check:
    while (j<nums.length) {
        let el=nums[j];
        if (el==undefined){
            nums.splice(j, 1);
             j=0;
        } else {
           j++;
        }
        
        
    }
    

    
};
