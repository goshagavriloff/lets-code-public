/**
 * @param {number[]} nums
 * @return {boolean}
 */
var containsDuplicate = function(nums) {
    let arr=Array.from(new Set(nums).values());
    return nums.length!=arr.length;
    
};
let costumF=(a,b)=>{
    return a-b;
}